version: "3"
services:
    frontend:
        build:
            context: ./client
            dockerfile: Dockerfile
        ports:
            - "4000:4000"
        
    backend:
        build:
            context: ./backend
            dockerfile: Dockerfile
        ports:
            - "9000:9000"
        
